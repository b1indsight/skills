interface:
  display_name: "Plan-First Feature Workflow"
  short_description: "Plan and get approval on a draft PR before writing code"
  default_prompt: "Use $plan-first-workflow to drive this feature: write a plan, open a draft PR, wait for explicit approval, then implement test-first on the same bookmark and PR."
policy:
  allow_implicit_invocation: true
